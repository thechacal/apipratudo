# Architecture

O apipratudo expoe /v1 via api-gateway e usa quota-service para chaves e limites.
Os demais servicos (webhook-service, file-service, cep-service e developer-portal) estao planejados.
Diagramas em docs/diagrams/README.md.
