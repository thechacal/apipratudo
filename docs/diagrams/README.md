# Diagramas Mermaid

- O GitHub renderiza Mermaid no README e em arquivos `.mmd`.
- Para visualizar localmente, use uma extensao Mermaid Preview (VS Code) ou o mermaid-cli.

Exemplo com mermaid-cli:
```bash
mmdc -i docs/diagrams/context.mmd -o /tmp/context.svg
```
